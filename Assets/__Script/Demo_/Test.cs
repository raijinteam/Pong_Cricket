using System.Collections;
using System.Collections.Generic;

using UnityEngine;



public class Test : MonoBehaviour {


    ////[SerializeField] private float flt_ClampPostion;
    ////[SerializeField] private float flt_Motionspeed;
    ////private float flt_TargetPostion;
    ////private float flt_CurrentPostion;

    ////private void Update() {

    ////    if (Input.GetMouseButton(0)) {
    ////        Vector3 postion = Camera.main.ScreenToWorldPoint(Input.mousePosition);


    ////        flt_TargetPostion = postion.x;
    ////        flt_TargetPostion = Mathf.Clamp(flt_TargetPostion, -flt_ClampPostion, flt_ClampPostion);


    ////    }

    ////    flt_CurrentPostion = transform.position.x;
    ////    flt_CurrentPostion = Mathf.Lerp(flt_CurrentPostion, flt_TargetPostion, Time.deltaTime * flt_Motionspeed);
    ////    transform.position = new Vector3(flt_CurrentPostion, transform.position.y, transform.position.z);

    ////}


    //[SerializeField]public RecorderController recorderController;

  
       

    //void Update() {
    //    // Check for screenshot input, for example, pressing a specific key
    //    if (Input.GetKeyDown(KeyCode.S)) {
    //        // Trigger the recording
    //        recorderController.PrepareRecording();
    //        recorderController.StartRecording();
    //    }
    //}
}







